<% @tasks.each do |task| %>
  <ul>
    <li>
      <%= link_to task.name, task_path(task) %>
    </li>
  </ul>
<% end %>

<div class="digg_pagination">
  <%= will_paginate @tasks, :page_links => false%>
</div>
